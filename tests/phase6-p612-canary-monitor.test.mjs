import test from 'node:test';import assert from 'node:assert/strict';import {monitorCanary} from '../.build/packages/phase6-canary-monitor/src/index.js';
const p={maxObservationAgeMs:5000,maxOorDistanceBins:5,maxInventoryRiskFraction:.4,minForwardEvToHold:0};const base={observedAt:'2026-08-12T19:51:00.000Z',reconciliationPassed:true,positionExists:true,dataFresh:true,thesisState:'VALID',forwardEv:.01,inRange:true,oorDistanceBins:0,inventoryRiskFraction:.1,liquidityCollapse:false,globalKillSwitch:false,feesAccruedValue:.002,netPnlValue:.001};
test('P6-12 healthy reconciled canary may HOLD but never reshape/rebalance',()=>{const r=monitorCanary(base,p,'2026-08-12T19:51:01.000Z');assert.equal(r.decision,'HOLD');assert.equal(r.reshapeAllowed,false);assert.equal(r.rebalanceAllowed,false);});
test('P6-12 invalid thesis requests close and liquidity collapse escalates emergency',()=>{assert.equal(monitorCanary({...base,thesisState:'INVALIDATED'},p,'2026-08-12T19:51:01.000Z').decision,'CLOSE_REVIEW');assert.equal(monitorCanary({...base,liquidityCollapse:true},p,'2026-08-12T19:51:01.000Z').decision,'EMERGENCY_CLOSE');});
