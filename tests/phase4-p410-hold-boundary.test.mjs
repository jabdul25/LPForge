import test from 'node:test'; import assert from 'node:assert/strict'; import { assessHoldAndBoundary } from '../.build/packages/hold-boundary/src/index.js';
const p={lowerBinId:90,upperBinId:110};
test('P4-10 healthy in-range economics support HOLD',()=>{const r=assessHoldAndBoundary({position:p,activeBinId:100,survival15m:.9,survival30m:.85,feeVelocityPerHour:.01,expectedInventoryLossPerHour:.002,binVelocityPerMinute:.3,volatilityRisk:.2,interventionCost:.003,thesisValid:true});assert.equal(r.holdEligible,true);assert.ok(r.holdForwardValuePerHour>0);});
test('P4-10 near-boundary risk requests management review but does not itself prescribe rebalance',()=>{const r=assessHoldAndBoundary({position:p,activeBinId:91,survival15m:.35,survival30m:.4,feeVelocityPerHour:.01,expectedInventoryLossPerHour:.003,binVelocityPerMinute:8,volatilityRisk:.9,interventionCost:.0001,thesisValid:true});assert.equal(r.managementReviewRequired,true);assert.equal(r.boundarySide,'LOWER');assert.ok(r.reasonCodes.includes('BOUNDARY_RISK_HIGH'));assert.equal('action' in r,false);});
test('P4-10 invalid thesis cannot be treated as healthy hold',()=>{const r=assessHoldAndBoundary({position:p,activeBinId:100,survival15m:.9,survival30m:.9,feeVelocityPerHour:.02,expectedInventoryLossPerHour:0,binVelocityPerMinute:0,volatilityRisk:0,interventionCost:.01,thesisValid:false});assert.equal(r.holdEligible,false);});
