import test from 'node:test'; import assert from 'node:assert/strict'; import { assessOorAndInventory } from '../.build/packages/oor-inventory/src/index.js';
const p={lowerBinId:90,upperBinId:110};
test('P4-13 modest OOR with high revisit probability can remain a WAIT candidate',()=>{const r=assessOorAndInventory({position:p,activeBinId:88,expectedReturnProbability:.8,expectedReturnMinutes:20,binVelocityPerMinute:.5,regimeDanger:.1,volatileTokenValue:.2,numeraireValue:.8,maxAdverseTokenValue:.3,feeVelocityBeforeOor:.01,rebalanceCost:.001});assert.equal(r.rangeStatus,'OOR_BELOW');assert.equal(r.suggestedDisposition,'WAIT_CANDIDATE');});
test('P4-13 deep OOR plus volatile inventory and dangerous regime escalates to exit review',()=>{const r=assessOorAndInventory({position:p,activeBinId:50,expectedReturnProbability:.1,expectedReturnMinutes:180,binVelocityPerMinute:9,regimeDanger:.95,volatileTokenValue:.85,numeraireValue:.15,maxAdverseTokenValue:1,feeVelocityBeforeOor:.002,rebalanceCost:.002});assert.equal(r.suggestedDisposition,'EXIT_REVIEW');assert.ok(r.inventoryRisk>.7);assert.ok(r.reasonCodes.includes('OOR_MANAGEMENT_URGENT'));});
test('P4-13 initial SOL-sided notion does not hide current volatile inventory',()=>{const r=assessOorAndInventory({position:p,activeBinId:100,expectedReturnProbability:1,expectedReturnMinutes:0,binVelocityPerMinute:0,regimeDanger:0,volatileTokenValue:.6,numeraireValue:.4,maxAdverseTokenValue:.8,feeVelocityBeforeOor:.01,rebalanceCost:0});assert.equal(r.inventoryExposureFraction,.6);assert.ok(r.reasonCodes.includes('INVENTORY_VOLATILE_ASSET_HIGH'));});
