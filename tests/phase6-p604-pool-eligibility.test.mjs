import test from 'node:test';import assert from 'node:assert/strict';import {assessMainnetPoolEligibility} from '../.build/packages/phase6-pool-eligibility/src/index.js';
const now='2026-08-12T19:43:00.000Z';const f={poolAddress:'pool-A',observedAt:now,activeBinId:5,tvlUsd:2_000_000,volume24hUsd:5_000_000,poolEnabled:true,protocolCompatible:true,tokenXBlacklisted:false,tokenYBlacklisted:false,tokenXFreezeRisk:false,tokenYFreezeRisk:false,liquidityCollapse:false,dataComplete:true};const p={allowedPools:['pool-A'],maxSnapshotAgeMs:5000,minTvlUsd:100_000,minVolume24hUsd:100_000,allowFreezeRisk:false};
test('P6-04 only explicit allowlisted healthy pool becomes canary eligible',()=>{assert.equal(assessMainnetPoolEligibility(f,p,now).decision,'ELIGIBLE');});
test('P6-04 blacklist, stale state, or liquidity collapse hard-blocks pool',()=>{const r=assessMainnetPoolEligibility({...f,tokenYBlacklisted:true,liquidityCollapse:true},p,'2026-08-12T19:43:10.000Z');assert.equal(r.decision,'BLOCK');assert.ok(r.reasonCodes.includes('P6_POOL_TOKEN_BLACKLIST'));assert.ok(r.reasonCodes.includes('P6_POOL_LIQUIDITY_COLLAPSE'));assert.ok(r.reasonCodes.includes('P6_POOL_STATE_STALE'));});
