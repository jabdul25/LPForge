import test from 'node:test';import assert from 'node:assert/strict';import { transitionOpportunity,deriveOpportunityProgress } from '../.build/packages/opportunity/src/index.js';
test('P3-08 state machine prohibits jumping directly from discovered to entry-ready',()=>{assert.throws(()=>transitionOpportunity('DISCOVERED','READY','2026-08-12T10:00:00Z'),/ILLEGAL_TRANSITION/);let s='DISCOVERED';for(const e of ['SCREEN','QUALIFY','ARM','READY'])s=transitionOpportunity(s,e,'2026-08-12T10:00:00Z').to;assert.equal(s,'ENTRY_READY');});
test('P3-08 ENTRY_READY remains recommendation-only',()=>{const x=transitionOpportunity('ARMED','READY','2026-08-12T10:00:00Z',['RANGE_CANDIDATE_POSITIVE']);assert.equal(x.recommendationOnly,true);assert.equal(x.to,'ENTRY_READY');});
test('P3-08 expired opportunity cannot remain qualified',()=>{const pool={eligibility:'ELIGIBLE',dataQuality:'GOOD',blockers:[]};const econ={economicallyPositive:true,uncertainty:.2,expectedActiveTimeRatio:.8,reasonCodes:[]};const regime={transitionRisk:.1};const x=deriveOpportunityProgress({pool,economics:econ,regime,now:'2026-08-12T11:00:00Z',expiresAt:'2026-08-12T10:59:00Z'});assert.equal(x.state,'EXPIRED');});
