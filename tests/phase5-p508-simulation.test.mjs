import test from 'node:test';import assert from 'node:assert/strict';import {simulateExecutionTransaction} from '../.build/packages/simulation-gateway/src/index.js';
const auth={phase:'P5',cluster:'devnet',level:'SIMULATE_ONLY',liveExecution:false,issuedAt:'2026-08-12T10:00:00Z',expiresAt:'2026-08-12T10:10:00Z',reasonCodes:[]};
test('P5-08 successful simulation records CU-derived limit and freshness',async()=>{const r=await simulateExecutionTransaction({authority:auth,transactionId:'tx1',transaction:{},simulatedAt:'2026-08-12T10:01:00Z',freshnessMs:30000,transport:{async simulate(){return{err:null,logs:['ok'],unitsConsumed:100000}}}});assert.equal(r.ok,true);assert.equal(r.recommendedComputeUnitLimit,110001);assert.equal(r.logs[0],'ok');});
test('P5-08 BUILD_ONLY authority cannot simulate',async()=>{await assert.rejects(()=>simulateExecutionTransaction({authority:{...auth,level:'BUILD_ONLY'},transactionId:'tx',transaction:{},simulatedAt:'2026-08-12T10:01:00Z',freshnessMs:30000,transport:{async simulate(){return{err:null}}}}),/AUTHORITY_DENIED/);});
test('P5-08 simulation errors are evidence, never approval',async()=>{const r=await simulateExecutionTransaction({authority:auth,transactionId:'tx2',transaction:{},simulatedAt:'2026-08-12T10:01:00Z',freshnessMs:30000,transport:{async simulate(){return{err:{InstructionError:[0,'Custom']},logs:['fail']}}}});assert.equal(r.ok,false);assert.match(r.error,/InstructionError/);});
