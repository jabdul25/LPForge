import test from 'node:test';import assert from 'node:assert/strict';import { analyzeRegimeHistory } from '../.build/packages/regime/src/index.js';
function a(t,primary,p1,p2){return{primary,probabilities:[{label:'SIDEWAYS',probability:p1},{label:'CONSOLIDATION',probability:p2},{label:'TRANSITION',probability:.1},{label:'CONTROLLED_PULLBACK',probability:.05},{label:'BREAKOUT',probability:.03},{label:'BREAKOUT_CONTROLLED_PULLBACK',probability:.02},{label:'TREND_UP',probability:.02},{label:'TREND_DOWN',probability:.02},{label:'DISTRIBUTION',probability:.01},{label:'EXHAUSTION',probability:.01},{label:'FREEFALL',probability:.01},{label:'RECOVERY',probability:.01},{label:'UNKNOWN',probability:Math.max(0,.72-p1-p2)}],confidence:Math.max(p1,p2),stability:.02,transitionRisk:.2,observedAt:t,reasonCodes:[],rawScores:{},evidence:{}};}
test('P3-05 minor label flip does not reset continuity duration',()=>{const h=[a('2026-08-12T10:00:00Z','SIDEWAYS',.38,.36),a('2026-08-12T10:05:00Z','CONSOLIDATION',.36,.38),a('2026-08-12T10:10:00Z','SIDEWAYS',.39,.35)];const x=analyzeRegimeHistory(h);assert.equal(x.labelChanges,2);assert.equal(x.continuityBreaks,0);assert.equal(x.stableDurationMinutes,10);});
test('P3-05 material distribution shift creates continuity break',()=>{const first=a('2026-08-12T10:00:00Z','SIDEWAYS',.55,.25);const second={...a('2026-08-12T10:05:00Z','SIDEWAYS',.5,.2),primary:'FREEFALL',probabilities:a('2026-08-12T10:05:00Z','SIDEWAYS',.05,.05).probabilities.map(p=>p.label==='FREEFALL'?{...p,probability:.7}:{...p,probability:p.probability*.3})};const x=analyzeRegimeHistory([first,second]);assert.ok(x.continuityBreaks>=1);assert.equal(x.stableDurationMinutes,0);assert.ok(x.downsideTransitionRisk>0);});
