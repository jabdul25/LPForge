import test from 'node:test';import assert from 'node:assert/strict';import {recommendComputeUnitLimit,estimateExecutionFee,assessExecutionCost} from '../.build/packages/execution-cost/src/index.js';
test('P5-07 compute limit uses simulated units plus 10 percent and clamps',()=>{assert.equal(recommendComputeUnitLimit(100000),110001);assert.equal(recommendComputeUnitLimit(1_390_000),1_400_000);});
test('P5-07 priority fee is ceil(CU price * CU limit / 1e6)',()=>{const e=estimateExecutionFee({signatureCount:2,computeUnitLimit:220000,computeUnitPriceMicroLamports:1000n});assert.equal(e.baseFeeLamports,10000n);assert.equal(e.priorityFeeLamports,220n);assert.equal(e.totalFeeLamports,10220n);});
test('P5-07 cost gate requires explicit policy and can reject economically silly execution',()=>{const e=estimateExecutionFee({signatureCount:1,computeUnitLimit:1_000_000,computeUnitPriceMicroLamports:100_000n});const r=assessExecutionCost(e,1_000_000n,{maxAbsoluteFeeLamports:50_000n,maxFeeFractionOfCapital:.02});assert.equal(r.approved,false);assert.ok(r.reasonCodes.includes('EXECUTION_FEE_ABSOLUTE_LIMIT'));});
