import test from 'node:test';import assert from 'node:assert/strict';
import {buildPhase7LiveControlDecision} from '../.build/packages/phase7-live-control/src/index.js';
const at='2026-08-14T00:00:00.000Z',authority={mode:'PRODUCTION',issuedAt:at,expiresAt:'2026-08-14T01:00:00.000Z',reasonCodes:[],approvalId:'approved',productionAuthorityIssued:true};
const health={status:'HEALTHY',observedAt:at,domainStatus:{RPC:'HEALTHY',METEORA_API:'HEALTHY',DATABASE:'HEALTHY',DECISION:'HEALTHY',EXECUTION:'HEALTHY',PORTFOLIO:'HEALTHY',RECONCILIATION:'HEALTHY'},staleDomains:[],missingDomains:[],reasonCodes:[],newEntriesAllowed:true,managementWritesAllowed:true,emergencyCloseAllowed:true};
const drift={status:'WATCH',reasonCodes:[],observedAt:at,sampleCount:100,newEntriesAllowed:true,managementAllowed:true,policyMutationAllowed:false,automaticPolicyPromotion:false,deltas:{}};
test('P7 production authority requires an exact approved release identity',()=>{const missing=buildPhase7LiveControlDecision({authority,health,drift,incidents:[]});assert.equal(missing.newEconomicActionAllowed,false);assert.ok(missing.reasonCodes.includes('P7_RELEASE_IDENTITY_MISSING'));const mismatch=buildPhase7LiveControlDecision({authority,health,drift,incidents:[],releaseIdentity:{valid:false,reasonCodes:['P7_RELEASE_SOURCE_MISMATCH']}});assert.equal(mismatch.newEconomicActionAllowed,false);assert.ok(mismatch.reasonCodes.includes('P7_RELEASE_SOURCE_MISMATCH'));const valid=buildPhase7LiveControlDecision({authority,health,drift,incidents:[],releaseIdentity:{valid:true,reasonCodes:[]}});assert.equal(valid.newEconomicActionAllowed,true);});
