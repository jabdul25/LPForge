import test from 'node:test'; import assert from 'node:assert/strict'; import { assessRebalance } from '../.build/packages/rebalance-intelligence/src/index.js';
test('P4-12 rebalance may change orientation when post-cost EV and inventory risk improve',()=>{const r=assessRebalance({currentForwardEv:.002,thesisStatus:'DETERIORATING',candidates:[{id:'new',strategy:'BID_ASK',orientation:'ONE_SIDED_X',lowerBinId:100,upperBinId:140,expectedGrossForwardValue:.014,implementationCost:.003,tailRiskCharge:.001,survivalProbability:.78,inventoryRiskBefore:.7,inventoryRiskAfter:.3,uncertainty:.25}]});assert.equal(r.decision,'REBALANCE');assert.equal(r.selectedCandidateId,'new');assert.ok(r.inventoryRiskImprovement>0);});
test('P4-12 high-cost churn stays NO_REBALANCE',()=>{const r=assessRebalance({currentForwardEv:.006,thesisStatus:'VALID',candidates:[{id:'x',strategy:'SPOT',orientation:'BALANCED',lowerBinId:90,upperBinId:130,expectedGrossForwardValue:.009,implementationCost:.004,tailRiskCharge:.001,survivalProbability:.9,inventoryRiskBefore:.4,inventoryRiskAfter:.3,uncertainty:.1}]});assert.equal(r.decision,'NO_REBALANCE');});
test('P4-12 invalid thesis must close/reduce path, not rebalance into a new expression',()=>{const r=assessRebalance({currentForwardEv:-.01,thesisStatus:'INVALIDATED',candidates:[]});assert.equal(r.decision,'NO_REBALANCE');assert.ok(r.reasonCodes.includes('REBALANCE_NOT_ALLOWED_AFTER_THESIS_INVALIDATION'));});
