import test from 'node:test';import assert from 'node:assert/strict';const a=await import('../.build/packages/accounting/src/index.js');test('exact raw token valuation avoids floating settlement math',()=>{const v=a.valueToken({mint:'x',raw:1_500_000n,decimals:6},{quote:'USD',value:'2.5',source:'FIXTURE',observedAt:'x'});assert.equal(v.ui,'1.5');assert.equal(v.value,'3.75');});test('position valuation includes fees and benchmark deltas',()=>{const v=a.valuePosition({positionAddress:'p',poolAddress:'q',tokenX:{mint:'x',raw:1_000_000n,decimals:6},tokenY:{mint:'y',raw:2_000_000n,decimals:6},priceX:{quote:'USD',value:'2',source:'F',observedAt:'x'},priceY:{quote:'USD',value:'1',source:'F',observedAt:'x'},feeX:{mint:'x',raw:100_000n,decimals:6},contributedValue:'4',hodlBenchmarkValue:'4.1'});assert.equal(v.grossValue,'4.2');assert.equal(v.absolutePnl,'0.2');assert.equal(v.hodlRelativePnl,'0.1');});

test('Meteora fee split and liquidity formulas use integer rounding',async()=>{const a=await import('../.build/packages/accounting/src/index.js');assert.equal(a.meteoraFeeFromAmountWithFee(1000n,10_000_000n),10n);assert.deepEqual(a.splitMmTradingFee(1000n,1000),{protocolFeeRaw:100n,lpFeeRaw:900n});assert.equal(a.liquidityShareFromDeposit(100n,1000n,2000n),50n);assert.equal(a.withdrawProRata(50n,1000n,1000n),50n);assert.equal(a.compositionFeeRaw(100000n,10_000_000n),1010n);});
