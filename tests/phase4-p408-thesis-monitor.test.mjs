import test from 'node:test'; import assert from 'node:assert/strict'; import { monitorThesis } from '../.build/packages/thesis-monitor/src/index.js';
const thesis={thesisId:'t',expectedEconomics:{netLpValue:.02},invalidation:['PULLBACK_SUPPORT_INVALIDATED']}; const regime={probabilities:[{label:'FREEFALL',probability:.05}],transitionRisk:.15,confidence:.7}; const risk={decision:'APPROVE',hardBlocks:[],emergencyReasons:[]};
const good={thesis,regime,currentForwardEv:.018,currentSurvivalProbability:.8,poolEligibility:'ELIGIBLE',toxicityProbability:.15,referenceDivergenceBps:20,supportIntegrity:.8,risk,observedAt:'2026-08-12T10:10:00Z'};
test('P4-08 thesis remains valid when forward economics and structure remain healthy',()=>{const r=monitorThesis(good);assert.equal(r.status,'VALID');assert.ok(r.improvedEvidence.includes('RANGE_SURVIVAL_ACCEPTABLE'));});
test('P4-08 deterioration is distinct from invalidation',()=>{const r=monitorThesis({...good,currentForwardEv:.004,currentSurvivalProbability:.5,toxicityProbability:.55});assert.equal(r.status,'DETERIORATING');assert.equal(r.invalidatedEvidence.length,0);});
test('P4-08 non-positive forward EV invalidates regardless of entry thesis',()=>{const r=monitorThesis({...good,currentForwardEv:-.001});assert.equal(r.status,'INVALIDATED');assert.ok(r.invalidatedEvidence.includes('FORWARD_EV_NON_POSITIVE'));});
test('P4-08 emergency risk overrides normal thesis state',()=>{const r=monitorThesis({...good,risk:{decision:'EMERGENCY',hardBlocks:[],emergencyReasons:['RISK_LIQUIDITY_COLLAPSE']}});assert.equal(r.status,'EMERGENCY');});
