import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {summarizeCanaryProgramme} from '../.build/packages/phase6-canary-program/src/index.js';
test('P6-15 aggregates repeated canaries without treating one success as production proof',()=>{const s=summarizeCanaryProgramme([{sessionId:'a',capitalLamports:10n,openReconciled:true,closeReconciled:true,failed:false,held:false,duplicateSubmissions:0,recoveryEvents:0,executionCostLamports:1n,feesEarnedValue:.1,netPnlValue:.05,maxInventoryRiskFraction:.2},{sessionId:'b',capitalLamports:10n,openReconciled:true,closeReconciled:true,failed:false,held:false,duplicateSubmissions:0,recoveryEvents:1,executionCostLamports:1n,feesEarnedValue:.2,netPnlValue:-.01,maxInventoryRiskFraction:.3}],20n);assert.equal(s.sessions,2);assert.equal(s.fullyReconciled,2);assert.equal(s.allSessionsTinyAndClosed,true);assert.equal(s.recoveryEvents,1);});
test('P6-15 M0018 persists sessions, observations and stage evidence',()=>{const sql=fs.readFileSync(new URL('../packages/db/migrations/M0018_phase6_canary_program.sql',import.meta.url),'utf8');for(const name of ['phase6_canary_sessions','phase6_canary_observations','phase6_stage_evidence'])assert.match(sql,new RegExp(name));});
