import test from 'node:test';import assert from 'node:assert/strict';import {mkdtempSync,writeFileSync,chmodSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {Keypair} from '@solana/web3.js';import {createLocalKeypairFileSigner} from '../.build/packages/phase6-mainnet-signer/src/index.js';
test('P6 local keypair-file signer requires owner-only JSON file, verifies its public key, and signs without exporting the secret',async()=>{const dir=mkdtempSync(join(tmpdir(),'lpforge-p6-')),path=join(dir,'owner.json'),keypair=Keypair.generate();try{writeFileSync(path,JSON.stringify([...keypair.secretKey]));chmodSync(path,0o600);const signer=createLocalKeypairFileSigner({backendId:'local-owner',publicKeyAddress:keypair.publicKey.toBase58(),keypairPath:path});const signature=await signer.signMessage(new Uint8Array([1,2,3]),{ticketId:'ticket',transactionId:'tx',cluster:'mainnet-beta'});assert.equal(signer.custodyMode,'LOCAL_KEYPAIR_FILE');assert.equal(signer.secretExportable,false);assert.equal(signature.byteLength,64);assert.throws(()=>createLocalKeypairFileSigner({backendId:'local-owner',publicKeyAddress:'11111111111111111111111111111111',keypairPath:path}),/OWNER_MISMATCH/);chmodSync(path,0o644);assert.throws(()=>createLocalKeypairFileSigner({backendId:'local-owner',publicKeyAddress:keypair.publicKey.toBase58(),keypairPath:path}),/PERMISSIONS/);}finally{rmSync(dir,{recursive:true,force:true});}});
