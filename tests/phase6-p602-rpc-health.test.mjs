import test from 'node:test';import assert from 'node:assert/strict';import {assessRpcHealth} from '../.build/packages/phase6-rpc-health/src/index.js';
const now='2026-08-12T19:41:00.000Z';const p={expectedGenesisHash:'mainnet-genesis',maxObservationAgeMs:10000,maxSlotSkew:5n,maxLatencyMs:800,requirePrivateWriteProvider:true,minHealthyProviders:2};
test('P6-02 requires private write provider and bounded cross-provider slot agreement',()=>{const a=assessRpcHealth([{providerId:'a',rpcClass:'DEDICATED_PRIVATE',observedAt:now,latencyMs:100,slot:100n,blockHeight:90n,genesisHash:'mainnet-genesis',healthy:true},{providerId:'b',rpcClass:'SECONDARY_PRIVATE',observedAt:now,latencyMs:120,slot:102n,blockHeight:91n,genesisHash:'mainnet-genesis',healthy:true}],p,now);assert.equal(a.status,'HEALTHY');assert.equal(a.writeEligible,true);});
test('P6-02 blocks wrong cluster or slot divergence',()=>{const a=assessRpcHealth([{providerId:'a',rpcClass:'DEDICATED_PRIVATE',observedAt:now,latencyMs:100,slot:100n,blockHeight:90n,genesisHash:'wrong',healthy:true},{providerId:'b',rpcClass:'SECONDARY_PRIVATE',observedAt:now,latencyMs:120,slot:120n,blockHeight:91n,genesisHash:'mainnet-genesis',healthy:true}],p,now);assert.equal(a.status,'BLOCK');assert.equal(a.writeEligible,false);});
