import test from 'node:test';import assert from 'node:assert/strict';import {reconcilePhase6Open} from '../.build/packages/phase6-open-reconciliation/src/index.js';
const ticket={ticketId:'tk',poolAddress:'pool-A',ownerAddress:'owner',action:'OPEN',maxLamports:20_000_000n,maxOpenPositions:1,issuedAt:'2026-08-12T19:50:00.000Z',expiresAt:'2026-08-12T19:51:00.000Z',policyHash:'p',autonomousScaling:false};const expected={planId:'plan',ticket,positionAddress:'pos1',lowerBinId:-10,upperBinId:10,maxNativeDebitLamports:25_000_000n};
test('P6-11 requires confirmed fresh exact PositionV2 truth before monitoring',()=>{const r=reconcilePhase6Open(expected,{observedAt:'2026-08-12T19:50:10.000Z',confirmed:true,dataFresh:true,position:{positionAddress:'pos1',exists:true,ownerAddress:'owner',poolAddress:'pool-A',lowerBinId:-10,upperBinId:10},nativeLamportsBefore:1_000_000_000n,nativeLamportsAfter:980_000_000n});assert.equal(r.status,'PASS');assert.equal(r.positionV2Verified,true);assert.equal(r.allowsMonitoring,true);});
test('P6-11 confirmed signature with wrong range is BLOCK',()=>{const r=reconcilePhase6Open(expected,{observedAt:'2026-08-12T19:50:10.000Z',confirmed:true,dataFresh:true,position:{positionAddress:'pos1',exists:true,ownerAddress:'owner',poolAddress:'pool-A',lowerBinId:-9,upperBinId:10},nativeLamportsBefore:1_000_000_000n,nativeLamportsAfter:980_000_000n});assert.equal(r.status,'BLOCK');assert.ok(r.reasonCodes.includes('RECON_LOWER_BIN_MISMATCH'));});
