import test from 'node:test';import assert from 'node:assert/strict';import { generateRangeUniverse } from '../.build/packages/rangeforge/src/index.js';
function input(vol='LOW',abs=10){return{activeBinId:1000,binStep:20,horizonMinutes:60,context:{horizons:{'5m':{absoluteBins:2,returnPct:0},'15m':{absoluteBins:abs,returnPct:.2},'30m':{},'1h':{absoluteBins:abs*2,binVelocityPerMinute:abs/60}},decisionAt:'x'},structure:{volatilityState:vol,trendDirection:0,trendEfficiency:.2},regime:{transitionRisk:.1}};}
test('P3-10 range universe is ordered, bin-native and capped by LPForge policy default',()=>{const u=generateRangeUniverse(input());assert.ok(u.candidates.length>=3);for(let i=1;i<u.candidates.length;i++)assert.ok(u.candidates[i].widthBins>=u.candidates[i-1].widthBins);assert.ok(u.candidates.every(c=>c.widthBins<=100&&c.lowerBinId<=1000&&c.upperBinId>=1000));assert.ok(u.candidates.every(c=>Number.isFinite(c.lowerDistancePct)&&Number.isFinite(c.upperDistancePct)));});
test('P3-10 volatility changes generated width rather than using one global fixed range',()=>{const low=generateRangeUniverse(input('LOW',8)),high=generateRangeUniverse(input('HIGH',30));assert.ok(high.candidates.at(-1).widthBins>low.candidates.at(-1).widthBins);});
test('P3-10 execution policy may cap RangeForge at 100 while Meteora supports larger extended positions',()=>{const u=generateRangeUniverse({...input('EXTREME',500),maxWidthBins:100,minWidthBins:69});assert.ok(u.candidates.length>=1);assert.ok(u.candidates.every(c=>c.widthBins<=100));});
