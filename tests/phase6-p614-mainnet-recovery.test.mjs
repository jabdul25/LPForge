import test from 'node:test';import assert from 'node:assert/strict';import {recoverMainnetCanary} from '../.build/packages/phase6-mainnet-recovery/src/index.js';
const journal={journalId:'j',idempotencyKey:'k',planId:'p',transactionId:'tx',state:'UNKNOWN_SUBMISSION',blockhash:'bh',lastValidBlockHeight:100,version:1,updatedAt:'2026-08-12T19:53:00.000Z',payload:{}};
test('P6-14 UNKNOWN while blockhash valid never permits another network send',()=>{const r=recoverMainnetCanary({journal,currentBlockHeight:90,confirmationStatus:'UNKNOWN',economicEffect:'UNKNOWN',reconciliationDebt:0,duplicateTrigger:false});assert.equal(r.action,'WAIT_DO_NOT_RESUBMIT');assert.equal(r.allowNetworkSend,false);});
test('P6-14 rebuild is permitted only after expiry and effect is proven absent',()=>{const r=recoverMainnetCanary({journal,currentBlockHeight:101,confirmationStatus:'EXPIRED',economicEffect:'ABSENT',reconciliationDebt:0,duplicateTrigger:false});assert.equal(r.action,'REBUILD_WITH_NEW_BLOCKHASH');assert.equal(r.allowNetworkSend,true);});
test('P6-14 on-chain effect forces reconciliation and duplicate trigger never sends',()=>{const r=recoverMainnetCanary({journal,currentBlockHeight:95,confirmationStatus:'CONFIRMED',economicEffect:'PRESENT',reconciliationDebt:0,duplicateTrigger:true});assert.equal(r.action,'RECONCILE_FIRST');assert.equal(r.allowNetworkSend,false);assert.ok(r.reasonCodes.includes('P6_RECOVERY_DUPLICATE_TRIGGER'));});
