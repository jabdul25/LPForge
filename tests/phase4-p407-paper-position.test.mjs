import test from 'node:test'; import assert from 'node:assert/strict'; import { transitionPaperPosition,rangeState,paperPositionValue } from '../.build/packages/paper-position/src/index.js';
const p={id:'p1',pool:'pool',token:'t',thesisId:'th',candidateId:'c',state:'PLANNED',capital:1,lowerBinId:90,upperBinId:110,tokenXValue:1,tokenYValue:0,feesValue:0,rewardsValue:0,costsValue:0,paperOnly:true,version:0};
test('P4-07 paper position follows gated lifecycle',()=>{let x=transitionPaperPosition(p,'MARK_ENTRY_READY','2026-08-12T10:00:00Z');x=transitionPaperPosition(x,'OPEN_REQUEST','2026-08-12T10:01:00Z');x=transitionPaperPosition(x,'OPEN_CONFIRMED','2026-08-12T10:02:00Z');x=transitionPaperPosition(x,'MARK_RANGE','2026-08-12T10:03:00Z',100);assert.equal(x.state,'IN_RANGE');assert.equal(x.paperOnly,true);});
test('P4-07 near/OOR states are derived from range facts',()=>{assert.equal(rangeState(90,110,90),'NEAR_LOWER_BOUND');assert.equal(rangeState(90,110,111),'OUT_OF_RANGE_ABOVE');assert.equal(rangeState(90,110,89),'OUT_OF_RANGE_BELOW');});
test('P4-07 illegal direct PLANNED to OPEN is impossible',()=>{assert.throws(()=>transitionPaperPosition(p,'OPEN_CONFIRMED','2026-08-12T10:00:00Z'),/ILLEGAL_TRANSITION/);});
test('P4-07 value includes fees/rewards and subtracts costs',()=>{assert.equal(paperPositionValue({...p,tokenXValue:.5,tokenYValue:.4,feesValue:.12,rewardsValue:.03,costsValue:.02}),1.03);});
