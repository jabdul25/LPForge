import test from 'node:test';import assert from 'node:assert/strict';import {deriveMainnetWalletTruth} from '../.build/packages/phase6-wallet-truth/src/index.js';
const base={ownerAddress:'11111111111111111111111111111111',cluster:'mainnet-beta',observedAt:'2026-08-12T19:42:00.000Z',slot:100n,nativeLamports:1_000_000_000n,tokenBalances:[],positions:[],activeReservations:[],reservedLamports:0n,availableLamports:1_000_000_000n,consistency:'CONSISTENT',reasonCodes:[]};
test('P6-03 preserves reserve before canary capital is considered available',()=>{const r=deriveMainnetWalletTruth(base,{minimumReserveLamports:500_000_000n,maximumCanaryLamports:50_000_000n,maxSnapshotAgeMs:10000,maxOpenCanaryPositions:1},'2026-08-12T19:42:01.000Z',0);assert.equal(r.consistent,true);assert.equal(r.availableAfterReserveLamports,500_000_000n);});
test('P6-03 blocks stale or already-open canary exposure',()=>{const r=deriveMainnetWalletTruth(base,{minimumReserveLamports:500_000_000n,maximumCanaryLamports:50_000_000n,maxSnapshotAgeMs:1000,maxOpenCanaryPositions:1},'2026-08-12T19:42:10.000Z',1);assert.equal(r.consistent,false);assert.ok(r.reasonCodes.includes('P6_WALLET_STALE'));assert.ok(r.reasonCodes.includes('P6_WALLET_POSITION_LIMIT'));});
