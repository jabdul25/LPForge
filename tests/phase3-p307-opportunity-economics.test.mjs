import test from 'node:test';import assert from 'node:assert/strict';import { estimateOpportunityEconomics } from '../.build/packages/opportunity/src/index.js';
const pool={pool:'p',policyId:'x',eligibility:'ELIGIBLE',poolQualityScore:80,economicQualityScore:85,flowQualityScore:80,liquidityQualityScore:80,tokenRiskScore:90,toxicityProbability:.15,archetype:'MATURE_DEEP',dataQuality:'GOOD',blockers:[],warnings:[],evidence:{},assessedAt:'2026-08-12T10:00:00Z'};
const probs=(freefall=.03)=>['SIDEWAYS','CONSOLIDATION','CONTROLLED_PULLBACK','BREAKOUT','BREAKOUT_CONTROLLED_PULLBACK','TREND_UP','TREND_DOWN','DISTRIBUTION','EXHAUSTION','FREEFALL','RECOVERY','TRANSITION','UNKNOWN'].map((label)=>({label,probability:label==='SIDEWAYS'?.45:label==='CONSOLIDATION'?.25:label==='FREEFALL'?freefall:(.27-freefall)/10}));
const regime={primary:'SIDEWAYS',probabilities:probs(),confidence:.45,stability:.2,transitionRisk:.1,observedAt:'2026-08-12T10:00:00Z',reasonCodes:[],rawScores:{},evidence:{}};
const structure={trendDirection:0,trendEfficiency:.2,impulseStrength:.2,retracementDepth:.2,compressionScore:.5,expansionScore:.1,supportIntegrity:.8,reclaimScore:.5,downsideAcceleration:.1,upsideAcceleration:.1,binVelocityAcceleration:0,flowTwoWay:.8,flowDirection:0,liquidityGapRisk:.1,liquiditySkewAbs:.1,volatilityState:'LOW',structureQuality:.8,reasonCodes:[]};
test('P3-07 favorable evidence can produce positive expected net LP value',()=>{const x=estimateOpportunityEconomics({capitalValue:100,horizonMinutes:120,rates:{feeRatePerCapitalHour:.01,adverseInventoryRatePerCapitalHour:.001,repositionRatePerCapitalHour:.0005,tailRiskRatePerCapitalHour:.0002,executionCostFixed:.02,sampleCount:100,uncertainty:.1,fidelity:'ONCHAIN_POSITION'},pool,regime,structure});assert.equal(x.economicallyPositive,true);assert.ok(x.expectedFeeValue>0);assert.ok(x.expectedNetLpValue>0);});
test('P3-07 high gross fee cannot hide larger adverse inventory economics',()=>{const x=estimateOpportunityEconomics({capitalValue:100,horizonMinutes:120,rates:{feeRatePerCapitalHour:.03,adverseInventoryRatePerCapitalHour:.08,repositionRatePerCapitalHour:.003,tailRiskRatePerCapitalHour:.003,executionCostFixed:.1,sampleCount:100,uncertainty:.1,fidelity:'ONCHAIN_POSITION'},pool,regime,structure});assert.equal(x.economicallyPositive,false);assert.ok(x.expectedFeeValue>0);assert.ok(x.expectedNetLpValue<0);assert.ok(x.reasonCodes.includes('EXPECTED_NET_VALUE_NON_POSITIVE'));});
