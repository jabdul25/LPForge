import test from 'node:test';import assert from 'node:assert/strict';import {Transaction,SystemProgram,PublicKey} from '@solana/web3.js';import {createEphemeralDevnetSignerHarness} from '../.build/packages/devnet-signing/src/index.js';
const auth={phase:'P5',cluster:'devnet',level:'DEVNET_SIGN',liveExecution:false,issuedAt:'2026-08-12T10:00:00Z',expiresAt:'2026-08-12T10:10:00Z',reasonCodes:[]};const risk={decision:'APPROVE',permitId:'permit',issuedAt:'2026-08-12T10:00:00Z',expiresAt:'2026-08-12T10:01:00Z',reasonCodes:[],hardBlocks:[],emergencyReasons:[]};
test('P5-11 first real signature is ephemeral and devnet-only',()=>{const h=createEphemeralDevnetSignerHarness({cluster:'devnet',allowEphemeralSigner:true});const from=new PublicKey(h.publicKeyAddress);const tx=new Transaction({recentBlockhash:'11111111111111111111111111111111',feePayer:from}).add(SystemProgram.transfer({fromPubkey:from,toPubkey:from,lamports:1}));const raw=h.signLegacyTransaction(tx,{authority:auth,riskDecision:risk,transactionId:'tx',signedAt:'2026-08-12T10:00:10Z'});assert.ok(raw.length>0);assert.equal(tx.signatures[0].signature?.length,64);assert.equal('secretKey' in h,false);});
test('P5-11 ephemeral signer can never be created for mainnet',()=>{assert.throws(()=>createEphemeralDevnetSignerHarness({cluster:'mainnet-beta',allowEphemeralSigner:true}),/DEVNET_ONLY/);assert.throws(()=>createEphemeralDevnetSignerHarness({cluster:'devnet',allowEphemeralSigner:false}),/EXPLICIT_ENABLE/);});
