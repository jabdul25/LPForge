import test from 'node:test'; import assert from 'node:assert/strict'; import { assessExit } from '../.build/packages/exit-intelligence/src/index.js';
const risk={decision:'APPROVE',hardBlocks:[],emergencyReasons:[]}; const oor={suggestedDisposition:'WAIT_CANDIDATE',inventoryRisk:.2,managementUrgency:.2};
test('P4-14 valid positive-forward position is not exited merely because exit engine ran',()=>{const r=assessExit({thesisStatus:'VALID',risk,currentForwardEv:.01,closeCost:.001,oor,drawdownFraction:.01});assert.equal(r.action,'HOLD');});
test('P4-14 thesis invalidation has distinct thesis exit reason',()=>{const r=assessExit({thesisStatus:'INVALIDATED',risk,currentForwardEv:.05,closeCost:.001,oor,drawdownFraction:0});assert.equal(r.action,'CLOSE_TO_NUMERAIRE');assert.equal(r.reasonFamily,'THESIS');});
test('P4-14 risk budget breach can REDUCE without pretending thesis failed',()=>{const r=assessExit({thesisStatus:'VALID',risk:{decision:'BLOCK',hardBlocks:['RISK_TOKEN_EXPOSURE_LIMIT'],emergencyReasons:[]},currentForwardEv:.01,closeCost:.001,oor,drawdownFraction:.02});assert.equal(r.action,'REDUCE');assert.equal(r.reasonFamily,'RISK');});
test('P4-14 emergency produces EMERGENCY_CLOSE',()=>{const r=assessExit({thesisStatus:'EMERGENCY',risk:{decision:'EMERGENCY',hardBlocks:[],emergencyReasons:['RISK_LIQUIDITY_COLLAPSE']},currentForwardEv:.1,closeCost:.005,oor,drawdownFraction:.1});assert.equal(r.action,'EMERGENCY_CLOSE');assert.equal(r.urgency,1);});
