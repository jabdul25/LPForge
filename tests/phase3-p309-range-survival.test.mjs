import test from 'node:test';import assert from 'node:assert/strict';import { fitRangeSurvivalModel,getSurvivalForecast } from '../.build/packages/range-survival/src/index.js';
const start=Date.parse('2026-08-12T00:00:00Z');const history=Array.from({length:301},(_,i)=>({observedAt:new Date(start+i*60000).toISOString(),activeBinId:100+Math.round(Math.sin(i/12)*8)}));
test('P3-09 wider empirical range has at least as much survival as narrow range on same path',()=>{const m=fitRangeSurvivalModel({history,fitThrough:history.at(-1).observedAt,horizonsMinutes:[30,60],ranges:[{id:'n',lowerOffsetBins:-3,upperOffsetBins:3},{id:'w',lowerOffsetBins:-12,upperOffsetBins:12}],anchorStride:5});const n=getSurvivalForecast(m,'n',30),w=getSurvivalForecast(m,'w',30);assert.ok(w.survivalProbability>=n.survivalProbability);assert.ok(w.expectedActiveTimeRatio>=n.expectedActiveTimeRatio);assert.ok(n.samples>0);});
test('P3-09 refuses fit history that contains observations after the fit cutoff',()=>{assert.throws(()=>fitRangeSurvivalModel({history,fitThrough:history[100].observedAt,horizonsMinutes:[30],ranges:[{id:'x',lowerOffsetBins:-5,upperOffsetBins:5}]}),/LOOKAHEAD/);});
