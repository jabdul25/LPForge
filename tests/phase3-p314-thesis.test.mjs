import test from 'node:test';import assert from 'node:assert/strict';import { generateLpThesis } from '../.build/packages/thesis/src/index.js';
const candidate={id:'winner',family:'BASE',lowerBinId:90,upperBinId:110,centerBinId:100,widthBins:21,lowerOffsetBins:-10,upperOffsetBins:10,lowerDistancePct:-1,upperDistancePct:1,reasonCodes:[],strategy:'BID_ASK',orientation:'ONE_SIDED_X',capitalFraction:.5,perBinWeights:[]};const pool={pool:'pool',policyId:'p',eligibility:'ELIGIBLE',poolQualityScore:80,economicQualityScore:80,flowQualityScore:80,liquidityQualityScore:80,tokenRiskScore:80,toxicityProbability:.1,archetype:'MATURE_DEEP',dataQuality:'GOOD',blockers:[],warnings:[],evidence:{},assessedAt:'x'};const regime={primary:'CONTROLLED_PULLBACK',probabilities:[{label:'CONTROLLED_PULLBACK',probability:.6},{label:'RECOVERY',probability:.2},{label:'SIDEWAYS',probability:.1}],confidence:.6,stability:.3,transitionRisk:.1,observedAt:'x',reasonCodes:[],rawScores:{},evidence:{}};const economics={horizonMinutes:120,expectedFeeValue:2,expectedRewardValue:0,expectedInventoryPnl:-.5,expectedHodlRelativePnl:-.6,expectedExecutionCost:.1,expectedRepositionCost:.1,expectedTailRiskCharge:.1,expectedNetLpValue:1.2,uncertainty:.2,capitalValue:100,expectedActiveTimeRatio:.8,favorableRegimeMass:.8,dangerousRegimeMass:.1,evidenceFidelity:'ONCHAIN_POSITION',evidenceSampleCount:50,economicallyPositive:true,reasonCodes:[]};const ranking={policyId:'r',winner:'winner',noTradeUtility:0,rankings:[],reasonCodes:['POSITIVE_RISK_ADJUSTED_CANDIDATE']};
test('P3-14 thesis is deterministic, machine-readable and contains invalidation contract',async()=>{const args={pool,regime,economics,candidate,ranking,survival:[],observedAt:'2026-08-12T10:00:00Z',expiresAt:'2026-08-12T10:05:00Z',provenance:{feature:'abc'}};const a=await generateLpThesis(args),b=await generateLpThesis(args);assert.equal(a.thesisId,b.thesisId);assert.equal(a.recommendationOnly,true);assert.equal(a.selectedCandidate.strategy,'BID_ASK');assert.ok(a.invalidation.includes('FORWARD_EV_NON_POSITIVE'));assert.ok(a.invalidation.includes('PULLBACK_SUPPORT_INVALIDATED'));assert.ok(a.expectedEconomics.netLpValue>0);});
test('P3-14 refuses to create trade thesis when NO_TRADE won',async()=>{await assert.rejects(()=>generateLpThesis({pool,regime,economics,candidate,ranking:{...ranking,winner:'NO_TRADE'},survival:[],observedAt:'x',expiresAt:'y'}),/REQUIRES_SELECTED_WINNER/);});
