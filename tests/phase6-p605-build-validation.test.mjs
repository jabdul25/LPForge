import test from 'node:test';import assert from 'node:assert/strict';import {issueReadBuildAuthority} from '../.build/packages/phase6-contracts/src/index.js';import {validateMainnetBuild} from '../.build/packages/phase6-build-validation/src/index.js';
const now='2026-08-12T19:44:00.000Z';const a=issueReadBuildAuthority({level:'MAINNET_BUILD_ONLY',now,ttlMs:60000});const plan={planId:'p1',cluster:'mainnet-beta',createdAt:now,expiresAt:'2026-08-12T19:45:00.000Z',state:'BUILT',reasonCodes:[],intent:{intentId:'i1',idempotencyKey:'k1',action:'OPEN',poolAddress:'pool-A',ownerAddress:'owner',thesisId:'t1',observedAt:now,expiresAt:'2026-08-12T19:45:00.000Z',payload:{}},transactions:[{transactionId:'tx1',sequence:0,kind:'METEORA_OPEN',requiredSignerAddresses:['owner'],writableAccounts:['pool-A'],state:'BUILT',metadata:{}}]};const pool={decision:'ELIGIBLE',poolAddress:'pool-A',observedAt:now,reasonCodes:[],activeBinId:5};
test('P6-05 binds build evidence to exact mainnet plan and pool snapshot without signing',()=>{const e=validateMainnetBuild({authority:a,plan,pool,builtAt:now});assert.equal(e.status,'PASS');assert.equal(e.signingPerformed,false);assert.match(e.planHash,/^p6build-/);});
test('P6-05 rejects cluster/pool mismatch',()=>{assert.throws(()=>validateMainnetBuild({authority:a,plan:{...plan,cluster:'devnet'},pool,builtAt:now}),/NOT_MAINNET/);assert.throws(()=>validateMainnetBuild({authority:a,plan,pool:{...pool,poolAddress:'other'},builtAt:now}),/POOL_MISMATCH/);});
