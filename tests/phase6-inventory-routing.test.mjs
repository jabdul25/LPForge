import test from 'node:test';import assert from 'node:assert/strict';import {planPhase6SolEntryFunding} from '../.build/packages/phase6-inventory-routing/src/index.js';
test('P6 lets a BidAsk winner create a SOL-sided range without purchasing token X',()=>{const r=planPhase6SolEntryFunding({strategy:'BID_ASK',capitalLamports:500_000_000n,activeBinId:100,lowerBinId:90,upperBinId:110});assert.equal(r.orientation,'ONE_SIDED_Y');assert.equal(r.solForLpLamports,500_000_000n);assert.equal(r.solToPairedTokenLamports,0n);assert.deepEqual([r.lowerBinId,r.upperBinId],[79,100]);});
test('P6 lets Spot or Curve winners fund a balanced position by converting half of its SOL capital',()=>{for(const strategy of ['SPOT','CURVE']){const r=planPhase6SolEntryFunding({strategy,capitalLamports:500_000_001n,activeBinId:100,lowerBinId:90,upperBinId:110});assert.equal(r.orientation,'BALANCED');assert.equal(r.solToPairedTokenLamports,250_000_000n);assert.equal(r.solForLpLamports,250_000_001n);}});
