import test from 'node:test'; import assert from 'node:assert/strict'; import { assessReshape } from '../.build/packages/reshape-intelligence/src/index.js';
const current={strategy:'CURVE',orientation:'BALANCED',lowerBinId:80,upperBinId:120,holdForwardEv:.004};
test('P4-11 reshape requires same thesis expression family and cost recovery',()=>{const r=assessReshape({current,thesisValid:true,candidates:[{id:'same',strategy:'CURVE',orientation:'BALANCED',lowerBinId:90,upperBinId:110,expectedGrossForwardValue:.012,implementationCost:.002,tailRiskCharge:.001,survivalProbability:.8,uncertainty:.2},{id:'different',strategy:'BID_ASK',orientation:'ONE_SIDED_X',lowerBinId:90,upperBinId:110,expectedGrossForwardValue:.05,implementationCost:.001,tailRiskCharge:0,survivalProbability:.9,uncertainty:.1}]});assert.equal(r.decision,'RESHAPE');assert.equal(r.selectedCandidateId,'same');assert.equal(r.eligibleCandidates.some(x=>x.id==='different'),false);});
test('P4-11 small gross improvement that cannot recover cost stays NO_RESHAPE',()=>{const r=assessReshape({current,thesisValid:true,candidates:[{id:'x',strategy:'CURVE',orientation:'BALANCED',lowerBinId:90,upperBinId:110,expectedGrossForwardValue:.005,implementationCost:.0015,tailRiskCharge:0,survivalProbability:.9,uncertainty:.1}]});assert.equal(r.decision,'NO_RESHAPE');assert.ok(r.reasonCodes.includes('RESHAPE_COST_NOT_RECOVERED'));});
test('P4-11 invalid thesis cannot be rescued by reshape',()=>{const r=assessReshape({current,thesisValid:false,candidates:[]});assert.equal(r.decision,'NO_RESHAPE');assert.ok(r.reasonCodes.includes('RESHAPE_REQUIRES_VALID_THESIS'));});
