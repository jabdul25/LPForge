import test from 'node:test'; import assert from 'node:assert/strict'; import { phase4PerformanceReport,decisionValueAdd } from '../.build/packages/phase4-evaluation/src/index.js';
test('P4-16 management performance is measured against no-management counterfactual, not win rate alone',()=>{const r=phase4PerformanceReport([{positionId:'a',entryDecision:'ENTER_NOW',managementAction:'RESHAPE',realizedNetValue:.02,holdCounterfactualNetValue:.01,noManagementCounterfactualNetValue:.008,entryDelayMinutes:0,firstOorMinutes:90,feesValue:.03,inventoryPnl:-.01,managementCost:.002},{positionId:'b',entryDecision:'WAIT',managementAction:'CLOSE_TO_NUMERAIRE',realizedNetValue:-.002,holdCounterfactualNetValue:-.02,noManagementCounterfactualNetValue:-.02,entryDelayMinutes:5,firstOorMinutes:20,feesValue:.005,inventoryPnl:-.012,managementCost:.001}]);assert.equal(r.n,2);assert.ok(r.managementValueAdd>0);assert.ok(r.feeToAdverseInventoryRatio>0);assert.equal(r.actionCounts.RESHAPE,1);});
test('P4-16 individual management decision value subtracts intervention cost',()=>{assert.equal(decisionValueAdd({action:'REBALANCE',actual:.012,counterfactual:.006,cost:.002}),.004);});
