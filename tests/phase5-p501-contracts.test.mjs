import test from 'node:test';import assert from 'node:assert/strict';
import {phase5Capabilities} from '../.build/packages/config/src/index.js';
import {assertAuthority} from '../.build/packages/execution-contracts/src/index.js';
test('P5-01 defaults to controlled build-only authority',()=>{const c=phase5Capabilities();assert.equal(c.phase,'P5');assert.equal(c.controlledExecution,true);assert.equal(c.defaultAuthority,'BUILD_ONLY');assert.equal(c.liveExecution,false);assert.equal(c.mainnetCanaryDefault,false);assert.equal(c.walletSecretInStrategy,false);assert.ok(c.prohibitedByDefault.includes('mainnet_submit'));});
test('P5-01 authority expires and cannot silently promote mainnet',()=>{assert.throws(()=>assertAuthority({phase:'P5',cluster:'mainnet-beta',level:'MAINNET_CANARY',liveExecution:false,issuedAt:'2026-08-12T10:00:00Z',expiresAt:'2026-08-12T10:05:00Z',reasonCodes:[]},['MAINNET_CANARY'],'2026-08-12T10:01:00Z'),/LIVE_EXECUTION_REQUIRED/);assert.throws(()=>assertAuthority({phase:'P5',cluster:'devnet',level:'BUILD_ONLY',liveExecution:false,issuedAt:'2026-08-12T10:00:00Z',expiresAt:'2026-08-12T10:00:30Z',reasonCodes:[]},['BUILD_ONLY'],'2026-08-12T10:01:00Z'),/AUTHORITY_EXPIRED/);});
