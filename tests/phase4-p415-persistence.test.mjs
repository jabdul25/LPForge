import test from 'node:test';import assert from 'node:assert/strict';import {persistPaperManagementCycleEvidence,runPaperManagementCycle} from '../.build/packages/paper-management/src/index.js';
const position={id:'persist-p1',pool:'pool',token:'tok',thesisId:'th',candidateId:'c',state:'IN_RANGE',capital:1,lowerBinId:90,upperBinId:110,openedAt:'2026-08-12T10:00:00Z',tokenXValue:.75,tokenYValue:.2,feesValue:.05,rewardsValue:0,costsValue:0,paperOnly:true,version:1};
const input={position,observedAt:'2026-08-12T10:15:00Z',activeBinId:100,thesisStatus:'VALID',riskFacts:{now:'2026-08-12T10:15:00Z',protocolCompatible:true,criticalDataObservedAt:'2026-08-12T10:14:30Z',dailyDrawdownFraction:.01,rollingDrawdownFraction:.02,tokenExposureFraction:.05,poolExposureFraction:.05,referenceDivergenceBps:10,liquidityChangeFraction:0},currentForwardEv:.008,actionForecasts:[{action:'HOLD',grossForwardValue:.009,implementationCost:0,tailRiskCharge:.001,uncertainty:.1,feasibility:1},{action:'RESHAPE',grossForwardValue:.0095,implementationCost:.002,tailRiskCharge:.001,uncertainty:.2,feasibility:1},{action:'CLOSE_TO_NUMERAIRE',grossForwardValue:0,implementationCost:.001,tailRiskCharge:0,uncertainty:0,feasibility:1}],expectedReturnProbability:.9,expectedReturnMinutes:0,binVelocityPerMinute:.2,regimeDanger:.1,volatileTokenValue:.2,numeraireValue:.8,maxAdverseTokenValue:.3,feeVelocityBeforeOor:.01,rebalanceCost:.002,closeCost:.001,drawdownFraction:.01};
test('P4-15 management evidence persists position, transition and decision without on-chain capability',async()=>{const result=runPaperManagementCycle(input);const calls=[];const store={async upsertPaperPosition(v){calls.push(['position',v]);},async insertPaperPositionEvent(v){calls.push(['event',v]);},async insertManagementDecision(v){calls.push(['decision',v]);}};await persistPaperManagementCycleEvidence(store,input,result);assert.deepEqual(calls.map(x=>x[0]),['position','event','decision']);assert.equal(calls[0][1].payload.paperOnly,true);assert.equal(calls[1][1].eventType,'MANAGEMENT_CYCLE');assert.equal(calls[2][1].managementDecisionId,'MGMT:persist-p1:2026-08-12T10:15:00Z');assert.equal(calls[2][1].action,result.managementAction);assert.equal(calls[2][1].payload.liveSigning,false);});
