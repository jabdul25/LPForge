import test from 'node:test';import assert from 'node:assert/strict';import { generateStrategyCandidates } from '../.build/packages/rangeforge/src/index.js';import { simulateCandidateSet } from '../.build/packages/candidate-simulator/src/index.js';
const g={id:'g',family:'BASE',lowerBinId:99,upperBinId:103,centerBinId:101,widthBins:5,lowerOffsetBins:-2,upperOffsetBins:2,lowerDistancePct:-1,upperDistancePct:1,reasonCodes:[]};const candidates=generateStrategyCandidates({universe:{activeBinId:101,binStep:10,horizonMinutes:60,candidates:[g],movementBasisBins:4,volatilityMultiplier:1},orientations:['BALANCED'],capitalFractions:[1]});
const bins=(shift=0)=>Array.from({length:7},(_,i)=>({pool:'p',binId:98+i,price:'1',amountX:String(1000+(i+shift)*20),amountY:String(1200-(i+shift)*15),liquiditySupply:'1000',stamp:{source:'FIXTURE',observedAt:'2026-08-12T10:00:00Z'}}));const frames=[{observedAt:'2026-08-12T10:00:00Z',activeBinId:101,bins:bins(0)},{observedAt:'2026-08-12T10:01:00Z',activeBinId:102,bins:bins(1)},{observedAt:'2026-08-12T10:02:00Z',activeBinId:104,bins:bins(2)}];const events=[{signature:'s',eventIndex:0,pool:'p',startBinId:101,endBinId:103,mmFee:'3000',feesOnTokenX:true,stamp:{source:'FIXTURE',observedAt:'2026-08-12T10:01:00Z'},raw:{}}];
test('P3-12 runs every strategy candidate through Phase 2 synthetic economics with explicit fidelity',()=>{const r=simulateCandidateSet({candidates,pool:'p',frames,events,totalPositionShareRaw:300n,rawUnitValueX:.001,rawUnitValueY:.001,capitalValue:1,costs:{transactionFeeValue:'.1'}});assert.equal(r.length,3);assert.deepEqual(new Set(r.map(x=>x.strategy)),new Set(['SPOT','CURVE','BID_ASK']));assert.ok(r.every(x=>x.fidelity==='EVENT_PATH_ESTIMATE'));assert.ok(r.every(x=>Number.isFinite(x.netValue)&&x.totalCostValue===.1));});
