import test from 'node:test'; import assert from 'node:assert/strict'; import { compareForwardEv } from '../.build/packages/forward-ev/src/index.js';
const f=(action,gross,cost=0,risk=0,uncertainty=.1,feasibility=1)=>({action,grossForwardValue:gross,implementationCost:cost,tailRiskCharge:risk,uncertainty,feasibility});
test('P4-09 HOLD wins when intervention cannot recover its cost',()=>{const r=compareForwardEv({positionId:'p',observedAt:'2026-08-12T10:00:00Z',thesisStatus:'VALID',forecasts:[f('HOLD',.01),f('RESHAPE',.011,.0015),f('CLOSE_TO_NUMERAIRE',0)]});assert.equal(r.action,'HOLD');});
test('P4-09 superior post-cost reshape can beat HOLD',()=>{const r=compareForwardEv({positionId:'p',observedAt:'2026-08-12T10:00:00Z',thesisStatus:'DETERIORATING',forecasts:[f('HOLD',.003),f('RESHAPE',.012,.002),f('CLOSE_TO_NUMERAIRE',0)]});assert.equal(r.action,'RESHAPE');assert.ok(r.incrementalBenefit>0);});
test('P4-09 thesis invalidation forces CLOSE when close alternative exists',()=>{const r=compareForwardEv({positionId:'p',observedAt:'2026-08-12T10:00:00Z',thesisStatus:'INVALIDATED',forecasts:[f('HOLD',.05),f('CLOSE_TO_NUMERAIRE',0)]});assert.equal(r.action,'CLOSE_TO_NUMERAIRE');});
