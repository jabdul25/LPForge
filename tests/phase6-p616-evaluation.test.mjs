import test from 'node:test';import assert from 'node:assert/strict';import {evaluatePhase6Exit,emptyPhase6Programme} from '../.build/packages/phase6-evaluation/src/index.js';
const p={minFullyReconciledCanaries:3,minForwardCycles:12,maxFailedCanaries:0,maxHeldCanaries:0,requireEmergencyStopEvidence:true,requireRpcRedundancy:true};
test('P6-16 implementation may PASS while operational promotion HOLDs without real mainnet canaries',()=>{const r=evaluatePhase6Exit({programme:emptyPhase6Programme(),liveForwardCycles:15,unresolvedReconciliationDebt:0,emergencyStopEvidence:true,rpcRedundancyHealthy:true,databaseRuntimeVerified:true,phase6BoundaryPass:true,localMeteoraLifecyclePass:true,mainnetCanaryEvidenceReal:false},p);assert.equal(r.implementationStatus,'PASS');assert.equal(r.operationalStatus,'HOLD');assert.equal(r.productionPromotion,'NOT_ELIGIBLE');assert.equal(r.productionAuthorityIssued,false);});
test('P6-16 limited-live eligibility requires repeated fully reconciled real canaries and still does not auto-scale',()=>{const programme={sessions:3,fullyReconciled:3,failed:0,held:0,duplicateSubmissions:0,recoveryEvents:1,totalCapitalLamports:30n,totalExecutionCostLamports:3n,totalFeesEarnedValue:.2,totalNetPnlValue:.1,maxInventoryRiskFraction:.3,allSessionsTinyAndClosed:true};const r=evaluatePhase6Exit({programme,liveForwardCycles:30,unresolvedReconciliationDebt:0,emergencyStopEvidence:true,rpcRedundancyHealthy:true,databaseRuntimeVerified:true,phase6BoundaryPass:true,localMeteoraLifecyclePass:true,mainnetCanaryEvidenceReal:true},p);assert.equal(r.operationalStatus,'PASS');assert.equal(r.productionPromotion,'LIMITED_LIVE_ELIGIBLE');assert.equal(r.autonomousScalingEnabled,false);assert.equal(r.productionAuthorityIssued,false);});
test('P6-16 duplicate submission evidence is a hard BLOCK',()=>{const programme={...emptyPhase6Programme(),sessions:3,fullyReconciled:3,duplicateSubmissions:1,allSessionsTinyAndClosed:true};const r=evaluatePhase6Exit({programme,liveForwardCycles:30,unresolvedReconciliationDebt:0,emergencyStopEvidence:true,rpcRedundancyHealthy:true,databaseRuntimeVerified:true,phase6BoundaryPass:true,localMeteoraLifecyclePass:true,mainnetCanaryEvidenceReal:true},p);assert.equal(r.operationalStatus,'BLOCK');});
