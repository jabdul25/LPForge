import test from 'node:test';import assert from 'node:assert/strict';import { calibrationReport,confusionReport,shadowPerformanceReport } from '../.build/packages/evaluation/src/index.js';
test('P3-16 calibration and confusion reports are deterministic and bounded',()=>{const c=calibrationReport([{probability:.8,outcome:1},{probability:.2,outcome:0},{probability:.7,outcome:1},{probability:.4,outcome:0}],5);assert.ok(c.brierScore>=0&&c.brierScore<=1);assert.ok(c.meanAbsoluteCalibrationError>=0&&c.meanAbsoluteCalibrationError<=1);const x=confusionReport([{predicted:'SIDEWAYS',actual:'SIDEWAYS'},{predicted:'FREEFALL',actual:'TREND_DOWN'}]);assert.equal(x.accuracy,.5);assert.equal(x.matrix.SIDEWAYS.SIDEWAYS,1);});
test('P3-16 shadow performance distinguishes false positives and missed opportunities',()=>{const r=shadowPerformanceReport([{recommendationId:'a',noTrade:false,realizedNetValue:-1,predictedSurvival:.8,survived:false},{recommendationId:'b',noTrade:false,realizedNetValue:2,predictedSurvival:.8,survived:true},{recommendationId:'c',noTrade:true,realizedNetValue:0,counterfactualBestNetValue:3}]);assert.equal(r.falsePositiveRate,.5);assert.equal(r.missedOpportunityRate,1);assert.equal(r.tradeRecommendations,2);assert.ok(r.survivalCalibration);});
