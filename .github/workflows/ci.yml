name: phase1-ci
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env: {POSTGRES_USER: lpforge, POSTGRES_PASSWORD: lpforge_ci, POSTGRES_DB: lpforge}
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U lpforge -d lpforge" --health-interval 5s --health-timeout 3s --health-retries 20
    env:
      DATABASE_URL: postgresql://lpforge:lpforge_ci@localhost:5432/lpforge
      SOLANA_RPC_HTTP_URL: https://api.mainnet-beta.solana.com
      LPFORGE_DATA_MODE: FIXTURE
      LIVE_SIGNING: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: "24.19.0", cache: "pnpm"}
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm test:ci
      - run: pnpm db:migrate
      - run: pnpm db:migrate
