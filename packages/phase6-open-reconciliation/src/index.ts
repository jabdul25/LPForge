// LPFORGE_PHASE6_MAINNET_MODULE
import {reconcileExecutionPlan,type PositionTruth} from '../../execution-reconciliation/src/index.js';
import type {Phase6CanaryTicket} from '../../phase6-contracts/src/index.js';
export interface Phase6OpenExpected {planId:string;ticket:Phase6CanaryTicket;positionAddress:string;lowerBinId:number;upperBinId:number;maxNativeDebitLamports:bigint;}
export interface Phase6OpenActual {observedAt:string;confirmed:boolean;dataFresh:boolean;position:PositionTruth;nativeLamportsBefore:bigint;nativeLamportsAfter:bigint;}
export interface Phase6OpenReconciliation {status:'PASS'|'HOLD'|'BLOCK';positionAddress:string;reasonCodes:string[];positionV2Verified:boolean;allowsMonitoring:boolean;rawStatus:'MATCH'|'MISMATCH'|'PARTIAL'|'UNKNOWN';}
export function reconcilePhase6Open(expected:Phase6OpenExpected,actual:Phase6OpenActual):Phase6OpenReconciliation{if(expected.ticket.action!=='OPEN')throw new Error('LPFORGE_P6_RECON_OPEN_TICKET');const r=reconcileExecutionPlan({planId:expected.planId,observedAt:actual.observedAt,expected:{action:'OPEN',ownerAddress:expected.ticket.ownerAddress,poolAddress:expected.ticket.poolAddress,newPositionAddress:expected.positionAddress,expectedLowerBinId:expected.lowerBinId,expectedUpperBinId:expected.upperBinId,maxNativeDebitLamports:expected.maxNativeDebitLamports},actual:{positions:[actual.position],nativeLamportsBefore:actual.nativeLamportsBefore,nativeLamportsAfter:actual.nativeLamportsAfter,confirmed:actual.confirmed,dataFresh:actual.dataFresh}});const positionV2Verified=r.status==='MATCH'&&actual.position.exists&&actual.position.positionAddress===expected.positionAddress;return{status:r.status==='MATCH'?'PASS':r.status==='UNKNOWN'?'HOLD':'BLOCK',positionAddress:expected.positionAddress,reasonCodes:r.discrepancies,positionV2Verified,allowsMonitoring:r.status==='MATCH',rawStatus:r.status};}
