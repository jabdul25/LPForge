// LPFORGE_PHASE6_MAINNET_MODULE
import {determineRecoveryAction,type ExecutionJournal,type RecoveryAction} from '../../execution-recovery/src/index.js';
export interface Phase6RecoveryFacts {journal:ExecutionJournal;currentBlockHeight:number;confirmationStatus:'PROCESSED'|'CONFIRMED'|'FINALIZED'|'EXPIRED'|'FAILED'|'UNKNOWN';economicEffect:'PRESENT'|'ABSENT'|'UNKNOWN';reconciliationDebt:number;duplicateTrigger:boolean;}
export interface Phase6RecoveryDecision {action:RecoveryAction;allowNetworkSend:boolean;allowNewEconomicAction:boolean;reasonCodes:string[];}
export function recoverMainnetCanary(f:Phase6RecoveryFacts):Phase6RecoveryDecision{const action=determineRecoveryAction(f);const reasons:string[]=[];if(f.duplicateTrigger)reasons.push('P6_RECOVERY_DUPLICATE_TRIGGER');if(f.reconciliationDebt>0)reasons.push('P6_RECOVERY_RECONCILIATION_DEBT');if(action==='WAIT_DO_NOT_RESUBMIT')reasons.push('P6_RECOVERY_BLOCKHASH_STILL_VALID');if(action==='RECONCILE_FIRST'||action==='MARK_RECONCILED')reasons.push('P6_RECOVERY_ONCHAIN_EFFECT_PRESENT');if(action==='REBUILD_WITH_NEW_BLOCKHASH')reasons.push('P6_RECOVERY_EXPIRED_EFFECT_PROVEN_ABSENT');if(action==='HOLD_FOR_OPERATOR')reasons.push('P6_RECOVERY_OPERATOR_HOLD');const allowNetworkSend=action==='REBUILD_WITH_NEW_BLOCKHASH'&&!f.duplicateTrigger&&f.reconciliationDebt===0;const allowNewEconomicAction=(action==='NO_ACTION_COMPLETE'||action==='MARK_RECONCILED')&&f.reconciliationDebt===0&&!f.duplicateTrigger;return{action,allowNetworkSend,allowNewEconomicAction,reasonCodes:reasons.sort()};}
