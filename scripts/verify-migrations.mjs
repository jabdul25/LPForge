import { readdir,readFile } from 'node:fs/promises';import path from 'node:path';const dir=path.resolve('packages/db/migrations');const names=(await readdir(dir)).filter(n=>/^M\d{4}_.+\.sql$/.test(n)).sort();if(names.length<6)throw new Error('expected >=6 migrations');for(const n of names){const s=await readFile(path.join(dir,n),'utf8');if(!/BEGIN;/i.test(s)||!/COMMIT;/i.test(s))throw new Error(`migration not transactional: ${n}`);if(/CREATE\s+TABLE(?!\s+IF\s+NOT\s+EXISTS)/i.test(s))throw new Error(`CREATE TABLE missing IF NOT EXISTS: ${n}`);}console.log(`MIGRATION_STATIC_OK ${names.join(',')}`);
