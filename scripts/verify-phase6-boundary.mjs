import fs from 'node:fs';import path from 'node:path';
const roots=['packages','apps'];const allowed=['packages/phase6-mainnet-signer/','packages/phase6-canary-runtime/','apps/canary/'];const forbidden=[/Keypair\.fromSecretKey\s*\(/,/sendRawTransaction\s*\(/,/sendTransaction\s*\(/,/sendAndConfirmTransaction\s*\(/];let scanned=0;const bad=[];
function walk(d){if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory()){if(['node_modules','.build','dist'].includes(e.name))continue;walk(p);}else if(e.isFile()&&p.endsWith('.ts')){scanned++;const rel=p.split(path.sep).join('/');if(!rel.includes('phase6')&&!rel.startsWith('apps/canary/'))continue;if(allowed.some(a=>rel.startsWith(a)))continue;const s=fs.readFileSync(p,'utf8');for(const re of forbidden)if(re.test(s))bad.push(`${rel}:${re}`);}}}
for(const r of roots)walk(r);if(bad.length){console.error('PHASE6_BOUNDARY_FAIL');for(const b of bad)console.error(b);process.exit(1);}console.log(`PHASE6_BOUNDARY_OK scanned=${scanned}`);
